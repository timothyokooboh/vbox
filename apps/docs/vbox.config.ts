import { defineConfig } from '@veebox/core';

export default defineConfig({
  theme: {
    color: {
      primary: '#FAF506',
      'bg-product': {
        default: '#fff',
        dark: '#22210E',
      },
      'text-product': {
        default: '#1c1c0d',
        dark: '#fff',
      },
    },
  },
});
