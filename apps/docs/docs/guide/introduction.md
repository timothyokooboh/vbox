# Introduction

## What is VBox?

VBox provides a polymorphic component that accepts CSS properties as attributes,
along with props for responsive design, dark mode, pseudo-states, keyframes,
etc. Under the hood, it generates CSS rules and injects the styles into the DOM.

<<< @/examples/Example1.vue

<Example1 />

<script setup lang="ts">
import Example1 from "../examples/Example1.vue"
</script>
